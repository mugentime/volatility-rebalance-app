# Flask Configuration
SECRET_KEY=your-secret-key-here
DEBUG=False
PORT=5000

# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/binance_strategy

# JWT Configuration  
JWT_SECRET_KEY=your-jwt-secret-key

# Binance API (these will be set per user in the application)
# BINANCE_API_KEY=your-binance-api-key
# BINANCE_API_SECRET=your-binance-api-secret
USE_TESTNET=False

# Security
ENCRYPTION_KEY=your-encryption-key-for-credentials

# Email Notifications (optional)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-email-password
FROM_EMAIL=your-email@gmail.com
ADMIN_EMAIL=admin@yourcompany.com

# Strategy Configuration
AUTOMATION_INTERVAL_MINUTES=5
MAX_SLIPPAGE_PERCENT=1.0
RATE_LIMIT_PER_MINUTE=60

# Logging
LOG_LEVEL=INFO

# CORS (for development)
CORS_ORIGINS=http://localhost:3000,http://localhost:5000
